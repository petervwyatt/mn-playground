[[Testing_tables]]
== Testing tables

=== Table styling

This tests table formatting. Due to ((zebra-striping)), rows are duplicated to test appearance for alternating rows. There are 2 header rows. See https://www.metanorma.org/author/topics/blocks/tables. This sentence also has a footnote footnote:[This is the footnote text from the first paragraph (not in the table).]

[[table1]]
.Title for table 1 **bold** __italic__ `mono`
[class="sortable",headerrows=2,cols="a,a,a,a",alt=Alt text for table accessibility,summary=Summary text for table accessibility]
|===
.2+|Sample 3+^| Formatting
| _Italic_ | *Bold* | `Monospaced`

| *Bold* | text | name a| Some text in a sentence with a footnote with blank {blank}footnote:[This is the first table footnote text preceded by blank.].

NOTE: This is a note inside a table cell. It has a long sentence so it hopefully spans several lines.

TIP: This is a tip inside a table cell. It also has a long sentence so it hopefully spans several lines.

.Source block in a table cell with **bold** __italic__ `mono`
[source,pdf,keep-lines-together=true]
----
2 0 obj
<<
  /KeyA (some value) % this is a very long PDF comment to see how wrapping of source code works with a table cell
>>
endobj
----

| *Bold* | text | name a| Some text in a sentence with a second footnote with empty {empty}footnote:[This is the second table footnote text preceded by empty.]

NOTE: This is a note inside a table cell. It has a long sentence so it hopefully spans several lines.

WARNING: This is a warning inside a table cell. It also has a long sentence so it hopefully spans several lines.

.HTML source block in a second table cell
[source,html,keep-lines-together=true]
----
<h1>Some heading</h1>
<div>
  <p style="font-size: 12pt; font-weight: normal;"><!-- this is a very long XML comment to see how wrapping of source code works with a table cell -->
  This is a very long paragraph of text in HTML to see how wrapping of source code works with a table cell. It should be colourized by Rouge.
  </p>
</div>
----

| Normal | text | string | A URL https://pdfa.org/resource/extension-pqc-signatures[title=Alt text for pqc URL via title attribute] in a cell.

| Normal | text | string | A URL https://pdfa.org/resource/extension-pqc-signatures in a cell without any markup.

| *Bla Blah* | text | name a| Some generic opening text in a sentence in a table cell. This cell needs to be repeated to test with zebra-striping.

CAUTION: This is a caution inside a table cell. It has a long sentence so it hopefully wrap and span more than one line.

.An example in a table cell
[example%collapsible]
====
This is random text within the example before some source code block

.Source block in a table cell with **bold** __italic__ `mono` in example block
[source,pdf,keep-lines-together=true]
----
2 0 obj
<<
  /KeyA (some value) % this is a very long PDF comment to see how wrapping of source code works with a table cell
>>
endobj
----

NOTE: Note within the example
====

And the final sentence of the example in a table cell.

| *Blah Bla* | text | name a| Some generic opening text in a sentence in a table cell. This cell needs to be repeated to test with zebra-striping.

CAUTION: This is a caution inside a table cell. It has a long sentence so it hopefully wrap and span more than one line.

.An example in a table cell
[example%collapsible]
====
This is random text within the example before some source code block

.Source block in a table cell with **bold** __italic__ `mono` in example block
[source,pdf,keep-lines-together=true]
----
2 0 obj
<<
  /KeyA (some value) % this is a very long PDF comment to see how wrapping of source code works with a table cell
>>
endobj
----

NOTE: Note within the example
====

And the final sentence of the example in a table cell.

|===

NOTE: This is a simple table note because it is not marked as `keep-separate`.

[NOTE]
====
This is another note but done as a block. It is also not marked as `keep-separate`.
====

[NOTE,keep-separate=true]
====
This is yet another note but done as a block. It _is_ marked as `keep-separate=true`!
====

This is a paragraph of normal text after the table. This is a paragraph of normal text after the table. This is a paragraph of normal text after the table. This is a paragraph of normal text after the table.


=== Testing a layout table

Citation to table below: <<table_layout_test>> tests a layout table for sub-figure block images with titles.

[[table_layout_test]]
.2x2 layout table
[cols="a,a",frame=none,grid=none,stripes=none,class="layout"]
|===

|
.Row 1, Column A
[figure]
====
image::test-cover.png[alt=some A1 alt text,title=title A1] 
====

|
.Row 1, Coumn B
[figure]
====
image::PDFaLogo.png[width=text-width,alt=some A2 alt text for T,title=title B1]
====

|
.Row 2, Column A
[figure%unnumbered]
====
image::Letter_T.png[alt=some B1 alt text for T,title=title A2]
====

|
.Row 2, Column B
[figure%unnumbered]
====
image::test-cover.png[width=text-width,alt=some B2 alt text,title=title B2]
====

|===

Random mention of term {{((EOD))}} for indexing purposes.

=== Testing sortable tables (HTML only)

The following <<table_sort_test>> should be sortable in HTML based on the `class` (not `role`!) attribute. Contents of this table also includes lists, definition lists, and examples. 

[[table_sort_test]]
.A random table from ISO 32000-2
[class="sortable",cols="~,~,a",options="header,autowidth"]
|===
h| Key | Type | Description

| *Start* | (various) | (_Optional_) The starting time span:requirement[shall] be nominally a non-negative 64-bit integer, specified as follows:

* If it is representable as a 32 bit integer, it span:requirement[shall] be specified as such.
** If it is not representable as a 32 bit integer, it span:requirement[shall] be specified as an 8-byte string representing a 64-bit twos-complement integer, most significant byte first.
* If it is expressed in a time scale different from that of the movie itself, it span:requirement[shall] be represented as an array of two values: an integer or byte string denoting the starting time, followed by an integer specifying the time scale in units per second. +
[NOTE]
This is a random note associated with last bullet above.

If this entry is omitted, the movie span:requirement[shall] be played from the beginning.

| *Duration* | (various) | (_Optional_) The duration of the movie segment to be played, that span:requirement[shall] be specified in the same form as *Start*. If this entry is omitted, the movie span:requirement[shall] be played to the end.

| *Rate* | number | (_Optional_) The initial speed at which to play the movie. If the value of this entry is negative, the movie span:requirement[shall] be played backward with respect to *Start* and *Duration*. 

Default value: 1.0.

| *ShowControls* | boolean | (_Optional_) A flag specifying whether to display a movie controller bar while playing the movie. 

Default value: span:pdf-keyword[false].

NOTE: Some random note.

| *Mode* | name | (_Optional_) The play mode for playing the movie:

_Once_:: Play once and stop.

_Open_:: Play and leave the movie controller bar open.

_Repeat_:: Play repeatedly from beginning to end until stopped.

_Palindrome_:: Play continuously forward and backward until stopped.

Default value: _Once_.

| *Synchronous* | boolean | (_Optional_) A flag specifying whether to play the movie synchronously or asynchronously. If this value is _true_, the movie player span:requirement[shall] retain control until the movie is completed or dismissed by the user. If the value is _false_, the player span:requirement[shall] return control to the interactive PDF processor immediately after starting the movie. 

Default value: span:pdf-keyword[false].

| *FWScale* | array | (_Optional_) The magnification (zoom) factor at which the movie span:requirement[shall] be played. The presence of this entry implies that the movie span:requirement[shall] be played in a floating window. If the entry is absent, the movie span:requirement[shall] be played in the annotation rectangle.

NOTE: Another random note in a table.

The value of the entry span:requirement[shall] be an array of two positive integers, _[numerator denominator]_, denoting a rational magnification factor for the movie. The final window size, in pixels, span:requirement[shall] be stem:[(ii"numerator" div ii"denominator") xx "Aspect"] where the value of *Aspect* span:requirement[shall] be taken from the movie dictionary.

| *FWPosition* | array | (_Optional_) For floating play windows, the relative position of the window on the screen. The value span:requirement[shall] be an array of two numbers _[horiz vert]_ each in the range 0.0 to 1.0, denoting the relative horizontal and vertical position of the movie window with respect to the screen.

[example%collapsible]
====
The value [ 0.5 0.5 ] centres the window on the screen.
====

Default value: [ 0.5 0.5 ].
|===


